<article class="feature-card">
  <slot />
</article>

<style>
  .feature-card {
    width: 11rem;
    height: 6rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    background-color: rgba(var(--color-quinary-rgb-value), 0.4);
    border-radius: 0.5rem;
    text-align: center;
    box-sizing: border-box;
  }

  .feature-card > :global(h3) {
    font-size: 1.5rem;
    font-weight: bold;
    margin: 0;
    color: var(--color-secondary);
  }

  .feature-card > :global(p) {
    font-size: .9rem;
    font-weight: 600;
    color: var(--color-background-light);
    text-wrap: wrap;
    width: 50%;
    text-align: center;
  }

  @media (width < 768px) {
    .feature-card {
      width: 9rem;
      height: 5rem;
    }

    .feature-card > :global(h3) {
      font-size: 1.3rem;
    }

    .feature-card > :global(p) {
      font-size: 0.8rem;
      width: 60%;
    }
  }

  @media (width < 480px) {
    .feature-card {
      width: 8rem;
      height: 4.5rem;
    }

    .feature-card > :global(h3) {
      font-size: 1.2rem;
    }

    .feature-card > :global(p) {
      font-size: 0.75rem;
      width: 70%;
    }
  }
</style>