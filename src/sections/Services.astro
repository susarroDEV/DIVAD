---
import ServiceCard from '../components/ServiceCard.astro'
import { services } from '../data/services.ts'
---

<section>
  <h1>
    Nuestros Servicios
  </h1>
  <div class="line-divider"/>
  <h2>
    En DIVAD Transformados Metálicos no hay límites para el metal. Fabricamos, reparamos y
adaptamos cualquier tipo de estructura metálica que necesites
  </h2>
  <ul>
    {services.map((service) => (
      <ServiceCard 
        {...service}
      />
    ))}
  </ul>
  </ul>
</section>

<style>
  section {
    background-color: rgba(var(--color-secondary-rgb-value), .05);
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 4rem 0;
  }

  h1 {
    font-size: 3.5rem;
    font-weight: bold;
    margin-bottom: 1rem;
    color: rgba(var(--color-quinary-rgb-value), .85);
  }

  h2 {
    font-size: 1.5rem;
    font-weight: 500;
    text-align: center;
    width: 70%;
    margin-bottom: 3rem;
    color: var(--color-quinary);
  }

  ul {
    width: 70%;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(20rem, 1fr));
    gap: 2rem;
  }

  .line-divider {
    margin-bottom: 2rem;
    width: 15%;
    height: 5px;
    background-color: rgba(var(--color-secondary-rgb-value), .8);
  }

  @media (width < 1024px) {
    ul {
      width: 85%;
      grid-template-columns: repeat(auto-fit, minmax(18rem, 1fr));
    }

    h2 {
      width: 85%;
    }
  }

  @media (width < 768px) {
    section {
      padding: 3rem 1rem;
    }

    h1 {
      font-size: 2.5rem;
    }

    h2 {
      font-size: 1.2rem;
      width: 90%;
      margin-bottom: 2rem;
    }

    ul {
      width: 95%;
      grid-template-columns: repeat(auto-fit, minmax(15rem, 1fr));
      gap: 1.5rem;
    }

    .line-divider {
      width: 25%;
      margin-bottom: 1.5rem;
    }
  }

  @media (width < 480px) {
    section {
      padding: 2rem 0.5rem;
    }

    h1 {
      font-size: 2rem;
      text-align: center;
    }

    h2 {
      font-size: 1.1rem;
      width: 95%;
    }

    ul {
      width: 100%;
      grid-template-columns: 1fr;
      gap: 1rem;
      padding: 0 0.5rem;
    }

    .line-divider {
      width: 30%;
    }
  }
</style>
